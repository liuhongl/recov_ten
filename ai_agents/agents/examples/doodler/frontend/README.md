## Doodler Frontend

This is the dedicated UI for the Doodler example.

### Prerequisites

- Node.js >= 20
- Bun (latest)

### Install dependencies

```bash
bun install
```

### Run dev server

```bash
bun run dev
```

The UI expects the API server at `http://localhost:8080` (override with `AGENT_SERVER_URL` if needed).
