[project]
name = "sip-realtime-voice-gateway"
version = "0.1.0"
description = "Standalone SIP realtime voice gateway skeleton"
requires-python = ">=3.11"
dependencies = [
  "websockets>=13.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
